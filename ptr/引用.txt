引用，就是变量的一个别名，对这个别名做的任何操作就是对它绑定的已存在的对象的操作。
引用在定义时必须要进行初始化，且它绑定的对象一定是一个已经存在的对象（有内存，可分配地址），引用本身
是没有地址和内存大小的，它只是封装了指针的指向功能，本质就是指针常量，其也说明这个引用只能绑定一个对象
语法：数据类型 &引用名 = 变量名;
引用本身不是一个对象，它没有内存和地址，我们获取引用只是获取的是它绑定的对象，因此不能定义引用的引用
如 type && t = a;

